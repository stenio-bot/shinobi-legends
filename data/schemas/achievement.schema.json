{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Achievement",
  "description": "Conquista (data/achievements.json). Implementado no servidor: tools/export_tfs.py gera server/generated/lib/naruto_achievements.lua + scripts/naruto/achievements.lua a partir daqui — ver docs/sistemas/progressao-servidor.md, seção 'Conquistas', para storages/hooks/limitações.",
  "type": "object",
  "required": ["id", "name", "description", "category", "reward"],
  "additionalProperties": false,
  "properties": {
    "id": {"type": "string", "pattern": "^[a-z0-9_]+$"},
    "name": {"type": "string"},
    "description": {"type": "string"},
    "category": {"enum": ["kill", "boss", "quest", "exam", "exploration", "collection", "task", "daily", "level"]},
    "condition": {
      "type": "object",
      "description": "Como o servidor detecta a conquista (NarutoAchievements, server/generated/lib/naruto_achievements.lua) — os 10 'kind' abaixo estão todos implementados.",
      "properties": {
        "kind": {"enum": ["kill_count", "kill_specific", "quest_chain_complete", "grants_rank", "visit_zone", "collect_set", "task_count", "daily_streak", "level_reached", "collect_item_count"]},
        "target": {"type": "string", "description": "monster_id, zone, rank ou outro identificador conforme 'kind'"},
        "count": {"type": "integer", "minimum": 1}
      }
    },
    "reward": {
      "type": "object",
      "required": ["title", "ryo"],
      "properties": {
        "title": {"type": "string", "description": "título exibido acima do nome do jogador ao completar"},
        "ryo": {"type": "integer", "minimum": 0}
      }
    }
  }
}
