{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Achievement",
  "description": "Conquista (data/achievements.json). Só dados nesta missão de conteúdo — o servidor ainda não lê/concede conquistas (ver relatório da missão para o formato proposto de armazenamento/entrega).",
  "type": "object",
  "required": ["id", "name", "description", "category", "reward"],
  "additionalProperties": false,
  "properties": {
    "id": {"type": "string", "pattern": "^[a-z0-9_]+$"},
    "name": {"type": "string"},
    "description": {"type": "string"},
    "category": {"enum": ["kill", "boss", "quest", "exam", "exploration", "collection", "task", "daily", "level"]},
    "condition": {
      "type": "object",
      "description": "Documentário: como o servidor deve detectar a conquista, quando a lógica for implementada.",
      "properties": {
        "kind": {"enum": ["kill_count", "kill_specific", "quest_chain_complete", "grants_rank", "visit_zone", "collect_set", "task_count", "daily_streak", "level_reached", "collect_item_count"]},
        "target": {"type": "string", "description": "monster_id, zone, rank ou outro identificador conforme 'kind'"},
        "count": {"type": "integer", "minimum": 1}
      }
    },
    "reward": {
      "type": "object",
      "required": ["title", "ryo"],
      "properties": {
        "title": {"type": "string", "description": "título exibido acima do nome do jogador ao completar"},
        "ryo": {"type": "integer", "minimum": 0}
      }
    }
  }
}
